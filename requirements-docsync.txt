# Optional dependencies for advanced DocSync features (LangChain/LangGraph)
# The current CI action uses only the stdlib-based CLI and does not require these.
# Pin versions as needed when enabling advanced features.

# Core graph/orchestration (optional)
langgraph>=0.2.0
langchain>=0.2.0

# LLM clients (choose based on provider)
openai>=1.0.0
anthropic>=0.39.0

# Embeddings / Vector stores (optional)
faiss-cpu>=1.7.4
chromadb>=0.5.0
sentence-transformers>=2.5.0

tiktoken>=0.7.0
pydantic>=2.0.0
rich>=13.7.0
GitPython>=3.1.43
Typer>=0.12.3
Tenacity>=8.2.3
